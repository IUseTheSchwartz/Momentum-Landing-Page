[build]
  command = "bash -lc 'pwd && ls -la && node -v && npm -v && echo --- && cat package.json && echo --- && npm ci && npm run build'"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
